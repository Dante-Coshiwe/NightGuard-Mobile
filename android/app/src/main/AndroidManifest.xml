<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Background patrol recording. The tracking service is always started while the app is in
         the foreground (the guard taps Start Patrol), which is what lets it keep receiving fixes
         with ordinary while-in-use location permission — ACCESS_BACKGROUND_LOCATION is deliberately
         NOT requested, so no guard is ever sent to system Settings to grant "Allow all the time". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION is deliberately NOT requested, and must not be added back
         casually. Briefly in 1.16 it was, for a continuous device tracker, and Google Play Protect
         blocked the install outright as harmful: background location + a boot receiver + uploading
         location to a server + an activity hidden from recents is the signature it classifies as
         stalkerware. Patrol recording works on the plain while-in-use grant because the service is
         only ever started while the app is in the foreground. Keep it that way. -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- NFC relaxed to optional so GPS-only (no-NFC) devices can install and patrol by GPS. -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- MainActivity must keep android:windowSoftInputMode="adjustResize". Under forced
         edge-to-edge, Capacitor's SystemBars inset listener already lifts the WebView above the
         IME by padding its parent. adjustPan made the window ALSO slide up, so the keyboard height
         was subtracted twice and the black decor background showed through as a bar above the
         keyboard. Paired with Keyboard.resizeOnFullScreen=false in capacitor.config.json, which is
         the other half of that double-resize (it shrank the content view a second time).

         android:excludeFromRecents must stay OFF (1.8-1.20 set it to "true"). It hid the whole
         NightGuard task from the overview screen, so a guard who stepped out to WhatsApp had no
         way back: Recents was empty of it, and Back from WhatsApp landed on the launcher rather
         than returning to the shift. They had to go and find the launcher icon. It bought nothing
         while pinned either — lock task mode already blocks the recents button — so the only
         thing it ever affected was the sanctioned trip out. It is also one leg of the Play
         Protect stalkerware signature described in the comment above. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:lockTaskMode="if_whitelisted"
            android:noHistory="false"
            android:alwaysRetainTaskState="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.capacitorjs.plugins.localnotifications.LocalNotificationRestoreReceiver"
            android:exported="true"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.REBOOT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".GuardForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
        />

        <service
            android:name=".PatrolTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
        />


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
